{# src/Blogger/BlogBundle/Resources/views/Comment/form.html.twig #}

<form action="{{ path('blog_comment_create', { 'article_id' : comment.article.id } ) }}" method="post" {{ form_enctype(form) }} class="blogger">
    {{ form_widget(form) }}
    <p>
        <input type="submit" value="Submit">
    </p>
</form